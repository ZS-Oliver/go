用于记录做项目的时候遇到的坑

### 2018/08/12

1、配置文件报Failed to load ApplicationContext错误

解决方案：beans 头有问题

2、测试配置文件同源码配置文件一样即可，不需要特殊声明

3、配置文件的作用，spring，springmvc就是普通的spring配置，主要有依赖注入和面向切面
web.xml内配置applicationContext的位置，也就是项目启动时加载spring
spring中配置数据库连接池等，并导入shiro的配置文件
mvc中配置json

### 2018/08/13

1、项目内容

打算开发一个图片、视频分享的网站

2、泛型

一般用于继承，如T extends Collection 则这个T可以是所有的集合类型

3、反射

能通过类拿到构造方法和方法，并创建实例、执行方法

### 2018/08/14

1、静态变量

不能通过@Autowire注入，不能在静态方法中使用非静态的变量

2、Mybatis中对象的创建

需要数据库中字段的名字和类属性的名字一样，才能根据名字创建相应的对象

3、常看

spring、springmvc、servlet、shiro

### 2018/08/18

1、前端访问服务器出现url正确而数据接收不到的错误时，考虑跨域问题